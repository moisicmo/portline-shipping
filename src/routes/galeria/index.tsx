import { component$ } from '@builder.io/qwik';
import { Section } from '~/components/ui/Section';
export default component$(() => (<Section title="Galería de Imágenes"><p>Grid de imágenes próximamente.</p></Section>));
